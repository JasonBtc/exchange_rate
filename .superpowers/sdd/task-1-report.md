# Task 1 Report: 添加依赖并清理脚手架

## Status
DONE_WITH_CONCERNS

## Commit
- `6c9cfcbff222481ccc540d78de73e7ce22373b78` — chore: add getx/dio/get_storage/intl deps and clean scaffold

## Changes
- `pubspec.yaml`: Added `get: ^4.6.6`, `dio: ^5.7.0`, `get_storage: ^2.1.1`, `intl: ^0.19.0` under `dependencies:` after `cupertino_icons`.
- `pubspec.lock`: Regenerated by `flutter pub get`.
- `lib/main.dart`: Replaced default Flutter counter scaffold with the `PlaceholderApp` stub from the brief.
- `test/widget_test.dart`: Updated the default widget test that referenced the removed `MyApp` symbol so it exercises `PlaceholderApp` instead (unavoidable — otherwise `flutter analyze` failed with `creation_with_non_type` on `MyApp`, blocking Step 4 verification). Kept the change minimal.

## Commands and Real Output

### `flutter pub get`
```
Resolving dependencies...
Downloading packages...
  intl 0.19.0 (0.20.3 available)
  matcher 0.12.18 (0.12.20 available)
  meta 1.17.0 (1.18.3 available)
  package_config 2.2.0 (3.0.0 available)
  test_api 0.7.9 (0.7.13 available)
  vector_math 2.2.0 (2.4.0 available)
Got dependencies!
6 packages have newer versions incompatible with dependency constraints.
Try `flutter pub outdated` for more information.
```
Result: matches expected `Got dependencies!`; no version conflicts. The "newer versions available" notice is informational (SDK-pinned lower bounds), not an error.

### `flutter analyze` (initial)
```
Analyzing exchange_rate...
  error • The name 'MyApp' isn't a class • test/widget_test.dart:16:35 • creation_with_non_type
1 issue found. (ran in 2.6s)
```
Cause: The stock `test/widget_test.dart` referenced the removed `MyApp`/counter widgets.

### `flutter analyze` (after updating the test)
```
Analyzing exchange_rate...
No issues found! (ran in 2.2s)
```
Matches expected `No issues found!`.

## Concerns
- Brief listed only `pubspec.yaml`, `pubspec.lock`, `lib/main.dart` in the commit. `test/widget_test.dart` had to be updated as well because the default template test referenced `MyApp`, which Step 3 removes; without that fix `flutter analyze` fails and Step 4 cannot pass. I updated the test to a minimal `PlaceholderApp` smoke test and included it in the commit — this is a strictly scoped follow-on of the `main.dart` change, not new scope.
- `git status` before the commit showed unrelated modifications outside task scope (`ios/Flutter/Debug.xcconfig`, `ios/Flutter/Release.xcconfig`, untracked `ios/Podfile`). Those were left untouched and not staged, per the "do not touch anything outside scope" instruction. They likely came from a prior `pod install` / Xcode interaction and remain in the working tree for a later task or manual review.
- `intl` has `0.20.3` available; brief pins `^0.19.0`, honored exactly.
